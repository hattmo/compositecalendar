import React, { useState, useMemo } from "react";
import CalendarOptions from "./selectCal";
import { ICalendar } from "../../types";

interface IProps {
    calendarList: ICalendar[];
}

export default ({ calendarList }: IProps) => {

};
